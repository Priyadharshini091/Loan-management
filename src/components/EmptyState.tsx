export function EmptyState({ title }: { title: string }) {
  return <div className="rounded-lg border border-dashed border-sky-200 bg-white p-8 text-center font-semibold text-slate-500">{title}</div>;
}
